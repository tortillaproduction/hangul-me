# Hangul-me — Claude Code 指示書

このファイルはプロジェクト全体に適用される決まりごとです。
作業を始める前に必ず読み、以下のルールに従ってください。

---

## プロジェクトの目的

ドラマや映画で耳に残った「うろ覚えの韓国語の音」から、正しいハングル・読み・意味へたどり着き、
自分の単語帳に貯めていくアプリです。

最終的な目的は **ハングルを見て意味が分かり、声に出して読めるようになること**。
機能を足すかどうか迷ったら、「頭の中の音」と「ハングルという文字」を結びつけるのに役立つかで判断してください。

---

## 1. 用語はユビキタス言語一覧に従う

**`docs/ubiquitous-language.md` が用語の唯一の基準です。**

- コードの命名・UI文言・コミットメッセージ・ドキュメント・会話、すべてでこの表の用語を使う
- 表にある語を、理由なく別の言い方に置き換えない
  - 例: 「候補検索」「新しく調べる」は廃止済み。**「単語を調べる」** に統一している
- 表にない新しい概念が出てきたら、**実装より先に表へ追記**してから進める
- 呼び方を変えたいときは、表を更新してから実装に反映する（逆順にしない）

作業前に一度目を通し、迷ったら必ず参照してください。

---

## 2. 言語の使い分け

**やり取り（会話・説明・提案）は日本語で行います。**
一方で、**英語で表現すべき箇所は英語のまま**にします。

| 対象 | 言語 |
|---|---|
| 会話・説明・レビューコメント | 日本語 |
| 変数名・関数名・型名・パッケージ名 | 英語 |
| DBのテーブル名・カラム名 | 英語（スネークケース） |
| APIのパス・JSONのキー | 英語 |
| Gitのブランチ名 | 英語 |
| コミットメッセージ | 英語（件名）。補足が必要なら本文に日本語可 |
| コード内コメント | 日本語 |
| ドキュメント（README・docs配下） | 日本語 |
| UI文言 | 日本語（ユビキタス言語一覧に従う） |
| エラーメッセージ（ユーザー向け） | 日本語 |
| エラーメッセージ（ログ・開発者向け） | 日本語。ただし識別子は英語のまま |

日本語のコメントに英単語を混ぜるのは問題ありません（例: `// pg_trgm で曖昧検索する`）。

---

## 3. ブランチ運用

**コードの追加・修正は必ず新しいブランチで行います。`main` に直接コミットしないでください。**

### 手順

```bash
# 1. main を最新にしてからブランチを切る
git switch main
git pull

# 2. 作業用ブランチを作る（英語・kebab-case）
git switch -c feat/word-review-flow

# 3. 作業 → 確認（下の「完了の定義」を満たすこと）

# 4. コミット（件名は英語、命令形）
git add <変更したファイル>
git commit -m "Add spaced repetition flow for registered words"

# 5. push
git push -u origin feat/word-review-flow
```

### ブランチ名の付け方

| 接頭辞 | 用途 |
|---|---|
| `feat/` | 機能の追加 |
| `fix/` | 不具合の修正 |
| `refactor/` | 振る舞いを変えない整理 |
| `docs/` | ドキュメントのみの変更 |
| `chore/` | 設定・依存・ビルド周り |

### マージ後のあと始末

**マージ完了が確認できてから**、使い終わったブランチを削除します。
確認が取れないうちは削除しないでください。

```bash
# マージされたか確認する
gh pr view <PR番号> --json state,mergedAt

# state が MERGED であることを確認できたら削除する
git switch main
git pull
git branch -d feat/word-review-flow            # ローカル
git push origin --delete feat/word-review-flow # リモート
```

- `git branch -D`（強制削除）は使わない。`-d` が拒否する＝まだマージされていない合図です
- マージの確認が取れない場合は削除せず、その旨を報告してください

---

## 4. 変更したらドキュメントも更新する

**プロジェクトの内容に追加・修正・変更があったときは、同じブランチ・同じコミットで
`README.md` と `docs/folder-structure.md` に反映してください。**

| 変更したもの | 更新先 |
|---|---|
| ディレクトリ・ファイルの追加／移動／削除 | `docs/folder-structure.md` |
| セットアップ手順・必要な環境変数 | `README.md` と対応する `.env.example` |
| APIエンドポイントの増減・変更 | `README.md` のAPI一覧 |
| 技術スタック・採用判断の変更 | `README.md` |
| 新しい概念・用語 | `docs/ubiquitous-language.md`（変更履歴にも1行追記） |
| DBスキーマ | `db/migrations/` に新しいマイグレーションを追加し、`README.md` の設計判断も見直す |

ドキュメントの更新を「あとでまとめて」にしないでください。コードと一緒に出すのが原則です。

---

## 5. 完了の定義

pushする前に、変更した領域について以下が通ることを確認してください。

### バックエンド（`backend/`）

```bash
cd backend
go build ./...
go vet ./...
go test ./...     # Ginkgo + Gomega
gofmt -l .        # 何も出力されないこと
```

### フロントエンド（`frontend/`）

```bash
cd frontend
npm run typecheck
npm run lint
npm run build
```

### データベース（`db/`）

マイグレーションを変更したら、実際に流して確認します。

```bash
docker compose down -v      # 既存のボリュームを消してから
docker compose up -d db     # 初期化スクリプトが最初から走る
```

---

## 6. 設計上の約束

コードを書くときに守る、このプロジェクト固有の決まりです。

### バックエンド（DDD）

- 依存は常に内向き。`domain` は他の層を知らない
  - `domain` に `database/sql` や `net/http` を持ち込まない
  - 永続化・外部APIは `domain` が定義したインターフェースを `infra` が実装する
- `interface/http` はHTTPの都合（JSON・ステータスコード）だけを引き受け、判断は `usecase` に委ねる
- テストは `usecase` 〜 `domain` を対象に Ginkgo で書く。`infra` はテストダブルで差し替える

### 候補マッチング

- **DB検索を先に、AIは足りないときだけ。** この順序を崩さない
- AI が生成した候補は、必ず辞書DBと再照合してから利用する（重複した辞書エントリを作らない）
- AI呼び出しが失敗しても、DB候補だけで応答を返す。検索機能を落とさない

### 分析用カラム

- `initial_consonant` / `has_final_consonant` / `final_consonant` は
  `internal/domain/hangul` の `Analyze()` が唯一の生成元
- 手で値を書かない。ハングルを更新したら `ApplyPhonetics()` を呼び直す

### ログの粒度

- `search_queries` に残すのは **ユーザーが選択した候補だけ**。表示しただけの候補は記録しない
- 個別ページの閲覧は生ログ（90日）＋日次集計（恒久）の二段構え。この方針を変えない

### データベース

- ロケールは必ず UTF-8 系にする
  - `locale=C` だと pg_trgm がかなを単語構成文字として扱わず、かな入力の曖昧検索が効かなくなる
  - 確認: `SELECT show_trgm('あんにょん');` が空配列でないこと

### フロントエンド

- 背景・各種バーはニュートラルなダーク。**紫はブランドのアクセントとしてのみ**使う
- 既定テーマはダーク。ライトは `data-theme="hangulme-light"` で明示的に切り替える
- `fetch` は `src/api/` の中だけで呼ぶ。コンポーネントから直接叩かない
- 2つ以上の画面で使う部品は `components/`、特定機能専用は `features/<機能名>/`
- グラフは単一系列なら凡例を置かず値を直接ラベルする。色はブランドvioletの検証済みの段階を使う

---

## 7. やらないこと

- `main` への直接コミット・直接push
- ドキュメントを更新しないままのコード変更
- ユビキタス言語一覧にない独自の言い換え
- マージ確認が取れていないブランチの削除
- `.env` のコミット（`.env.example` だけを追跡する）
- 秘密情報（APIキー・接続文字列）のコード内へのベタ書き
